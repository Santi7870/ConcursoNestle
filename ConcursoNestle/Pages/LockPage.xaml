<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ConcursoNestle.Pages.LockPage"
             BackgroundColor="#F5F5F5">

    <VerticalStackLayout Padding="30" Spacing="20">
        <!-- Mensaje de bloqueo -->
        <Label Text="La aplicación está bloqueada"
               FontSize="24"
               HorizontalOptions="Center"
               TextColor="#FF3B30"
               FontAttributes="Bold"/>

        <!-- Información sobre el bloqueo -->
        <Label Text="{Binding BlockInfo}"
               FontSize="18"
               HorizontalOptions="Center"
               TextColor="Black"/>

        <!-- Botón de desbloqueo -->
        <Button Text="Desbloquear"
                FontSize="20"
                BackgroundColor="#0078D4"
                TextColor="White"
                CornerRadius="8"
                HeightRequest="50"
                VerticalOptions="CenterAndExpand"
                Clicked="OnUnlockButtonClicked"/>
    </VerticalStackLayout>
</ContentPage>


